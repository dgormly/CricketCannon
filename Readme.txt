
FOR FRESH INSTALL:
Must have Node.js v8 installed.

cd into folder and run 'npm install'

To run application, click RUN.bat in the CricketApp folder.

Output files are saved in root folder under 'data'.
Scale is plugged into COM 10;

Email me if anything stops working @ daniel.gormly@uqconnect.edu.au